# Example study

Analyses built from this work's data live in `research/`, one file per study; install.sh links each into
`~/.agents/research/`, where skills and agents can read them. This file is a placeholder.
